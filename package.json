{
  "name": "app",
  "private": true,
  "version": "1.0.0",
  "description": "This is a demo MERN application that exposes condo management reports through a role-based web portal. It implements:",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently -n SERVER,CLIENT \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "install:all": "npm i && npm --prefix server i && npm --prefix client i && npm --prefix launcher i",
    "start": "npm --prefix server start",
    "start:server": "npm --prefix server start",
    "start:client": "npm --prefix client start",
    "desktop:image:build": "node ./scripts/desktop/build-image.mjs",
    "desktop:image:push": "node ./scripts/desktop/push-image.mjs",
    "desktop:image:publish": "node ./scripts/desktop/publish-image.mjs",
    "launcher:start": "npm --prefix launcher run start",
    "launcher:package": "npm --prefix launcher run package",
    "launcher:package:win": "npm --prefix launcher run package:win",
    "launcher:package:mac": "npm --prefix launcher run package:mac",
    "launcher:package:linux": "npm --prefix launcher run package:linux",
    "desktop:release:win": "node ./scripts/desktop/assemble-release.mjs win32",
    "desktop:release:mac": "node ./scripts/desktop/assemble-release.mjs darwin",
    "desktop:release:linux": "node ./scripts/desktop/assemble-release.mjs linux",
    "desktop:bundle:win": "npm run launcher:package:win && npm run desktop:release:win",
    "desktop:bundle:mac": "npm run launcher:package:mac && npm run desktop:release:mac",
    "desktop:bundle:linux": "npm run launcher:package:linux && npm run desktop:release:linux"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
