name: condo-desktop

services:
  app:
    image: ${CONDO_APP_IMAGE}
    restart: unless-stopped
    ports:
      - "3000:5000"
    env_file:
      - ./.env.docker
    environment:
      - PORT=5000
      - SEED_ON_START=true
      - MONGO_URI=mongodb://mongo:27017/condo_app
      - JWT_SECRET=change_me_in_production
      - CLIENT_URL=http://localhost:3000
      - GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback
      - MICROSOFT_CALLBACK_URL=http://localhost:3000/api/auth/microsoft/callback
      - REPORTING_PROVIDER=mysql
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=condo_mgmt
      - MYSQL_USER=condo_app
      - MYSQL_PASSWORD=condo_app_pw
    depends_on:
      mongo:
        condition: service_healthy
      mysql:
        condition: service_healthy

  mongo:
    image: mongo:7
    restart: unless-stopped
    volumes:
      - mongo-data:/data/db
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'mongosh --quiet --eval "db.adminCommand(''ping'').ok" mongodb://localhost:27017/admin | grep 1',
        ]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  mysql:
    image: ${CONDO_DB_IMAGE}
    restart: unless-stopped
    env_file:
      - ./.env.docker
    environment:
      - MYSQL_ROOT_PASSWORD=root_password
      - MYSQL_DATABASE=condo_mgmt
      - MYSQL_USER=condo_app
      - MYSQL_PASSWORD=condo_app_pw
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "mysqladmin ping -h 127.0.0.1 -uroot -p$${MYSQL_ROOT_PASSWORD} --silent",
        ]
      interval: 5s
      timeout: 5s
      retries: 60
      start_period: 60s

volumes:
  mongo-data:
  mysql-data:
